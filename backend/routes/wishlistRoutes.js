// import express from 'express';
// import { getWishList, addToWishList, removeWishListItem } from '../controllers/wishlistControllers.js';
// import { protect } from '../middleware/authMiddleware.js';

// const wishlistRouter = express.Router();

// // fetch the wishlist
// wishlistRouter.get('/', protect, getWishList);

// // add a book to the wishlist
// wishlistRouter.post('/add', protect, addToWishList);

// // remove a book from wishlist
// wishlistRouter.delete('/remove/:productId', protect, removeWishListItem);

// export default wishlistRouter;
