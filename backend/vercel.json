{
	"version": 2,
	"builds": [
		{
			"src": "./server.js",
			"use": "@vercel/node"
		}
	],
	"routes": [
		{
			"src": "/(.*)",
			"dest": "/"
		}
	],
	"scripts": [
		{
			"install": "npm install",
			"build": "npm run build"
		}
	]
}
